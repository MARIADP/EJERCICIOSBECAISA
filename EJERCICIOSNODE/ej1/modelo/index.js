//hay que crear este indez porque es necesaria que e la caroeta exista un index.js para que lo lea el de fueta sino no lo lee

const libros = require('./libros');

module.exports = { 
     libros: libros
};